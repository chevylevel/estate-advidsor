import styled from '@emotion/styled';
import { css } from '@emotion/react';

const catalogueStyles = () => css`

`

const StyledCatalogue = styled('div')(catalogueStyles);

export default StyledCatalogue;
