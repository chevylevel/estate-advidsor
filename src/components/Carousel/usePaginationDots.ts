import { useEffect, useRef } from 'react';

const usePaginationDots = ({
    current,
    total,
}) => {

    useEffect(
        () => {

        },
        [current],
    );

    return {

    };
};

export default usePaginationDots;
